/**
 * 根据obj返回一个新对象，其每个key由obj的键值对经过iteratee映射后生成，但其每个value维持和obj一致
 * @param obj，原始对象
 * @param iteratee，用于将obj的键值对映射为一个新的key
 * @return 返回一个将映射后的key、obj的value当做键值对的新对象
 */
declare function mapKeys(obj: Object | any[], iteratee?: Function | string): Object;
export default mapKeys;
