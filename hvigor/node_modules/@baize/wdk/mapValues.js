"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var utils_1 = require("./common/utils");
var keys_1 = __importDefault(require("./keys"));
/**
 * 根据obj返回一个新对象，其每个value由obj的键值对经过iteratee映射后生成，但其每个key维持和obj一致
 * @param obj，原始对象
 * @param iteratee，用于将obj的键值对映射为一个新的value
 * @return 返回一个将obj的key、映射后的value当做键值对的新对象
 */
function mapValues(obj, iteratee) {
    var realIteratee = (0, utils_1.getRealIterateeWithIdentityDefault)(iteratee);
    var objKeys = (0, keys_1.default)(obj);
    var result = {};
    objKeys.forEach(function (key) {
        result[key] = realIteratee(obj[key], key, obj);
    });
    return result;
}
exports.default = mapValues;
