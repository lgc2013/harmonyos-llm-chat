/**
 * 裁剪数组/类数组array，从 rawStart 位置开始到 rawEnd 结束，但不包括 rawEnd 本身的位置
 * 返回 数组array 裁剪部分的新数组
 * @param array 要裁剪数组
 * @param rawStart 开始位置
 * @param rawEnd   结束位置
 */
export default function slice(array: any, rawStart?: any, rawEnd?: any): any[];
