"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * 检查value在array数组中首次被找到的索引值
 * @param array
 * @param value
 * @param start（如果start为负值，将从数组array尾端索引进行匹配）
 */
function indexOf(array, value, start) {
    if (array === null || array === undefined) {
        return -1;
    }
    var startIndex = Number.isNaN(Number(start)) ? 0 : Number(start);
    startIndex = Math.round(Number(startIndex) < 0 ? Math.max(array.length + Number(startIndex), 0) : startIndex);
    for (var i = startIndex, len = array.length; i < len; i++) {
        if (array[i] === value || (Number.isNaN(value) && Number.isNaN(array[i]))) {
            return i;
        }
    }
    return -1;
}
exports.default = indexOf;
