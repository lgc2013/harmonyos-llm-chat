/**
 * 创建一个切片数组，去除array后面的n个元素, n默认为1。
 *
 * @param array 要处理的数组
 * @param n 要去除的元素个数
 * @returns {Array} 数组剩余切片
 */
export default function dropRight<T>(array: T[] | null | undefined, n?: number): T[];
