"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * 基础函数，对array进行切片，从rawStart索引开始，到rawEnd结束（左闭右开）
 * 注意该函数和wdk.slice略有不同，其为提升性能做了简化，未考虑非法类型的输入也没有默认值
 * @param array，要切片的数组
 * @param rawStart，切片起始索引
 * @param rawEnd，切片终止索引
 * @return 返回切片后的数组
 */
function baseSlice(array, rawStart, rawEnd) {
    var length = array.length;
    var start = rawStart;
    if (start < 0) {
        start = -rawStart > length ? 0 : (length + rawStart);
    }
    var end = rawEnd > length ? length : rawEnd;
    if (end < 0) {
        end += length;
    }
    // 计算得到result数组的长度
    length = start > end ? 0 : end - start;
    var result = Array(length);
    var index = -1;
    while (++index < length) {
        result[index] = array[index + start];
    }
    return result;
}
exports.default = baseSlice;
