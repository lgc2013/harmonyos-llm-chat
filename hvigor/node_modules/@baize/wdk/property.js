"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var isKey_1 = require("./common/isKey");
var toKey_1 = require("./common/toKey");
var get_1 = __importDefault(require("./get"));
/**
 * 创建一个取值函数，该函数能够获取目标 object 上指定 path 的值
 *
 * @param {Array|string} path 路径信息
 * @returns {Function} 取值函数
 * @example
 *
 * const objects = [
 *   { 'a': { 'b': 2 } },
 *   { 'a': { 'b': 1 } }
 * ]
 *
 * map(objects, property('a.b'))
 * // => [2, 1]
 *
 * map(sortBy(objects, property(['a', 'b'])), 'a.b')
 * // => [1, 2]
 */
function property(path) {
    if ((0, isKey_1.isKey)(path)) {
        return function (object) { return object === null || object === void 0 ? void 0 : object[(0, toKey_1.toKey)(path)]; };
    }
    return function (object) { return (0, get_1.default)(object, path); };
}
exports.default = property;
