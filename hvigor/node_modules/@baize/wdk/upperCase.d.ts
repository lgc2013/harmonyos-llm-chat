/**
 * 将指定的字符串转换成空格分离的大写字母。
 * @param [str=''] (string): 待转换的字符串
 * @returns 空格分开的大写字母字符串
 * @example
 *
 * ```ts
 * _.upperCase('--foo-bar');
 * // => 'FOO BAR'
 *
 * _.upperCase('fooBar');
 * // => 'FOO BAR'
 *
 * _.upperCase('__foo_bar__');
 * // => 'FOO BAR'
 * ```
 *
 */
export default function upperCase(str?: any): string;
