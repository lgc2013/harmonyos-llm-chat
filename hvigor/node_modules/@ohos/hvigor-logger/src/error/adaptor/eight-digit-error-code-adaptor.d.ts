import { ErrorAdaptor } from '../abstract-adaptor.js';
import { AdaptorErrorMessage } from '../type.js';
/**
 * 适配新的八位错误码格式
 *
 * ERROR: 21030400 ArkTS Compiler Error
 * Error Message: The `maxOldSpace` configured in file hvigor-config.json5 is greater than 2000. At file: D://path/to/the/invalid/file/hvigor-config.json5
 *
 * * Try the following:
 *   > Adjusting `maxOldSpace` and make sure the value cannot be greater than 2000.
 *   > More info: https://123/cn/2234
 */
export declare class EightDigitErrorCodeAdaptor extends ErrorAdaptor {
    private readonly _errorMsg;
    constructor(errorMsg: string);
    /**
     * 通过正则表达式匹配错误代码，并将所有匹配到的结果放以数组形式返回
     *
     * @return 返回一个错误代码的数组
     */
    private getErrorInfos;
    getErrorMessage(): AdaptorErrorMessage[];
}
