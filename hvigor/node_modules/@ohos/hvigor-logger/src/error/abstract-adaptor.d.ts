import { AdaptorErrorMessage } from './type.js';
/**
 * 适配器模式,将不同格式的错误通过适配器形成统一的输出形式
 */
export declare abstract class ErrorAdaptor {
    /**
     * 对外暴露错误数据
     */
    abstract getErrorMessage(): AdaptorErrorMessage[];
}
/**
 * 统一管理有json文件的适配器
 */
export interface ErrorManagerAdaptor {
    /**
     * 错误管理要有对应的错误信息存储文件路径
     * 一个适配器可能管理着多个json文件
     */
    getErrorCodeJsonPath(): string[];
}
