import { HvigorLogInfo } from './hvigor-log-info.js';
/**
 * 基于log4js封装的HvigorLogger，直接进行日志文件的输出。
 * 日志打印的核心类，只负责将标准HvigorLogInfo对象进行打印适配log4js打印不负责message转义
 */
export declare class HvigorLoggerForFile {
    private static hvigorLoggerCache;
    private readonly fileLogger;
    private constructor();
    /**
     * 获取对应类别的HvigorLoggerForFile实例
     *
     * @param {string} category 默认是default
     * @return {HvigorLoggerForFile}
     */
    static getLogger(category: string): HvigorLoggerForFile;
    static getLoggerWithDurationId(category: string, durationId: string): HvigorLoggerForFile;
    debug(logInfo: HvigorLogInfo): void;
    info(logInfo: HvigorLogInfo): void;
    warn(logInfo: HvigorLogInfo): void;
    error(logInfo: HvigorLogInfo): void;
    errorAndExit(logInfo: HvigorLogInfo): void;
}
