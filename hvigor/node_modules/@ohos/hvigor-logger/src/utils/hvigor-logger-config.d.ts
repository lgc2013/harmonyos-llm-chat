import { Configuration, Level } from 'log4js';
export declare class HvigorLoggerConfig {
    private static extraConfig;
    private static hvigorCacheDir;
    private static configuration;
    private static logLevel;
    static setExtraConfig(value: Map<string, string>): void;
    static getExtraConfig(key: string): string | undefined;
    static setHvigorCacheDir(value: string | undefined): void;
    static getHvigorCacheDir(): string | undefined;
    static setConfiguration: (config: Configuration) => void;
    static getConfiguration: () => Configuration;
    /**
     * 更新log4js的配置
     * @returns
     */
    static updateConfiguration(): Configuration;
    /**
     * 将各Category的日志级别进行统一调整（白名单及file类型的不涉及）
     * @param level
     * @param ignoreLevelCategoryFilterArr
     */
    static setCategoriesLevel(level: Level, ignoreLevelCategoryFilterArr?: string[]): void;
    /**
     * 获取当前的日志级别
     * @returns
     */
    static getLevel(): Level;
}
