/**
 * 通过一个递归实现对象的深冻结,对象本身和内部嵌套的其他子对象均进行冻结
 * 不能添加新的属性。
 * 不能删除现有的属性。
 * 不能修改现有属性的值、可枚举性（enumerable）、可配置性（configurable）、可写性（writable）。
 * 对象的原型（__proto__）也不能被修改。
 *
 * @param object 待冻结的对象
 */
export declare const freezeDeep: (object: any) => any;
