/**
 * 将字符串键的Map对象转换为普通对象。
 *
 * @param {Map<string, any>} strMap - 需要转换的Map对象，其中键为字符串，值可以是任何类型。
 * @returns {object} 转换后的普通对象，如果Map中有嵌套的Map对象，将递归转换。
 */
export declare function strMapToObj(strMap: Map<string, any>): object;
